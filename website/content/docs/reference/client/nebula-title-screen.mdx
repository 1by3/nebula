---
title: "NebulaTitleScreen"
description: "Displays an IMGUI panel where a player can enter the gateway address and a display name before connecting."
---

<div className="text-sm text-fd-muted-foreground">Class in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Client/NebulaTitleScreen.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Client/NebulaTitleScreen.cs)</div>

```csharp
public sealed class NebulaTitleScreen : MonoBehaviour
```

Displays an IMGUI panel where a player can enter the gateway address and a display name before connecting. Nebula skips the panel when `-nebula-gateway`, `-nebula-bot`, or `-nebula-connect` supplies the connection. The selected values persist in PlayerPrefs.

## Fields

### Client [#client]

```csharp
public NebulaClient Client;
```

## Methods

### TryParse [#tryparse]

```csharp
public static bool TryParse(string text, out string address, out ushort port);
```

