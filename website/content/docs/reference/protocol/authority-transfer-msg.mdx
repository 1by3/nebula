---
title: "AuthorityTransferMsg"
description: "The authority event."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct AuthorityTransferMsg
```

The authority event. Carries the sender's exact final state so the receiver snaps to it and never computes a starting state of its own, plus the not-yet-simulated inputs so the input stream continues without a gap.

## Fields

### Entity [#entity]

```csharp
public EntitySpawnMsg Entity;
```

### NewEpoch [#newepoch]

```csharp
public uint NewEpoch;
```

### PendingInputs [#pendinginputs]

```csharp
public byte[] PendingInputs;
```

### HandoverState [#handoverstate]

```csharp
public byte[] HandoverState;
```

Per-behavior handover-only state ([`NetworkIdentity.WriteHandoverState`](/docs/reference/core/network-identity#writehandoverstate)).

### GhostWorkers [#ghostworkers]

```csharp
public string[] GhostWorkers;
```

### SessionGeneration [#sessiongeneration]

```csharp
public ulong SessionGeneration;
```

The owner's session as the sender knew it ([`PlayerSessions`](/docs/reference/protocol/player-sessions)), so the receiver accepts the owner's gateway at once. 0/"" for an unowned entity.

### SessionGateway [#sessiongateway]

```csharp
public string SessionGateway;
```

### InterestGateways [#interestgateways]

```csharp
public string[] InterestGateways;
```

Gateway keys ([`PlayerSessions.GatewayKey`](/docs/reference/protocol/player-sessions#gatewaykey)) that were following this entity on the old owner without subscribing its region — an explicit per-entity subscription, or the session it speaks for. The new owner announces the spawn to these gateways as well as to the gateways its own regions cover, so a followed entity is not lost the moment it changes worker.

### Crossing [#crossing]

```csharp
public bool Crossing;
```

The entity is crossing a physics frame's boundary and the sender is not the frame's pose owner, so it hands the entity over unconverted for the pose owner to cross it at its exact pose (`docs/container-tree.md` D15). A trailing optional byte: an older reader stops before it.

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter w);
```

### Read [#read]

```csharp
public static AuthorityTransferMsg Read(NetworkReader r);
```
