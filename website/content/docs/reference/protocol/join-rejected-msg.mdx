---
title: "JoinRejectedMsg"
description: "Gateway -> client: the join was refused; Reason is fit to show the player."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct JoinRejectedMsg
```

Gateway -\> client: the join was refused; [`Reason`](#reason) is fit to show the player. The gateway disconnects after sending it.

## Fields

### Reason [#reason]

```csharp
public string Reason;
```

### Retry [#retry]

```csharp
public bool Retry;
```

Whether the client should retry automatically, such as when this gateway is draining. Reaching another gateway requires a configured load balancer or another routing mechanism. When false, inspect [`Code`](#code) and [`Reason`](#reason) before reconnecting.

### Code [#code]

```csharp
public JoinRejectReason Code;
```

The typed refusal reason. Defaults to [`JoinRejectReason.None`](/docs/reference/protocol/join-reject-reason#none) when omitted.

### Saturation [#saturation]

```csharp
public float Saturation;
```

How saturated the target was, for [`JoinRejectReason.AtCapacity`](/docs/reference/protocol/join-reject-reason#atcapacity): 1 = the whole of the dominant component's budget ([`CapacityInfo.Saturation`](/docs/reference/orchestrator/capacity-info#saturation)). Sent as an f16; 0 when unknown.

### SupportedMinVersion [#supportedminversion]

```csharp
public ushort SupportedMinVersion;
```

The gateway's inclusive supported protocol range, supplied on every refusal. Both limits are 18. Zero means the corresponding field was omitted.

### SupportedMaxVersion [#supportedmaxversion]

```csharp
public ushort SupportedMaxVersion;
```

The gateway's inclusive supported protocol range, supplied on every refusal. Both limits are 18. Zero means the corresponding field was omitted.

### ServerContentVersion [#servercontentversion]

```csharp
public uint ServerContentVersion;
```

The gateway's configured game content version, supplied on every refusal. Zero means content version checks are disabled or the field was omitted.

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter w);
```

### Read [#read]

```csharp
public static JoinRejectedMsg Read(NetworkReader r);
```
