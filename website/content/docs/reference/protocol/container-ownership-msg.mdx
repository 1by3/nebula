---
title: "ContainerOwnershipMsg"
description: "Carries the container ownership protocol message."
---

<div className="text-sm text-fd-muted-foreground">Static class in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public static class ContainerOwnershipMsg
```

Carries the container ownership protocol message.

## Methods

### Write [#write]

```csharp
public static void Write(NetworkWriter w, IList<ContainerOwnershipEntry> entries);
public static void Write(NetworkWriter w, IList<ContainerOwnershipEntry> entries, bool full, IList<string> removes);
```

Write a complete snapshot (the form a gateway sends on join and on every control-plane change).

### Read [#read]

```csharp
public static ContainerOwnershipUpdate Read(NetworkReader r);
```
