---
title: "EntityDespawnMsg"
description: "Carries the entity despawn protocol message."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct EntityDespawnMsg
```

Carries the entity despawn protocol message.

## Fields

### NetId [#netid]

```csharp
public ulong NetId;
```

### Epoch [#epoch]

```csharp
public uint Epoch;
```

### ViewSeq [#viewseq]

```csharp
public ushort ViewSeq;
```

Gateway -\> client: the view this despawn ends (see [`EntitySpawnMsg.ViewSeq`](/docs/reference/protocol/entity-spawn-msg#viewseq)). A client drops a despawn older than the view it currently holds, so a late leave cannot kill a re-entered replica. Workers send 0.

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter w, MsgId id);
```

### Read [#read]

```csharp
public static EntityDespawnMsg Read(NetworkReader r);
```
