---
title: "MsgId"
description: "Wire message ids."
---

<div className="text-sm text-fd-muted-foreground">Enum in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public enum MsgId : byte
```

Wire message ids. One byte header on every packet. The same entity message shapes are used on all three links (client-gateway, gateway-worker, worker-worker); only the id tells the receiver which role sent it.

## Members

| Name | Value | Description |
| --- | --- | --- |
| <span id="hello"></span>`Hello` | `1` |  |
| <span id="welcome"></span>`Welcome` | `2` |  |
| <span id="ping"></span>`Ping` | `3` |  |
| <span id="pong"></span>`Pong` | `4` |  |
| <span id="joinstatus"></span>`JoinStatus` | `5` | Gateway -\> client: how the join is going ([`JoinStatusMsg`](/docs/reference/protocol/join-status-msg)). Sent once the client is welcomed and again whenever the answer changes, so a mesh that has scaled to zero can hold the client in a "world starting" state instead of leaving it welcomed with no pawn and no explanation. |
| <span id="joinrejected"></span>`JoinRejected` | `6` | Gateway -\> client: the join was refused ([`JoinRejectedMsg`](/docs/reference/protocol/join-rejected-msg): the token in `Hello` did not verify, or the mesh requires one). The gateway disconnects right after sending it. |
| <span id="gatewaydraining"></span>`GatewayDraining` | `7` | Gateway -\> client: this gateway is being taken out of service ([`GatewayDrainingMsg`](/docs/reference/protocol/gateway-draining-msg)). The client should reconnect, presenting its session token, so another gateway takes over its session without the worker noticing more than a pause in input. |
| <span id="sessionreplaced"></span>`SessionReplaced` | `8` | Gateway -\> client: a newer connection for this player took the session over ([`SessionReplacedMsg`](/docs/reference/protocol/session-replaced-msg)). The gateway disconnects right after sending it. |
| <span id="entityspawn"></span>`EntitySpawn` | `10` |  |
| <span id="entitydespawn"></span>`EntityDespawn` | `11` |  |
| <span id="entityvars"></span>`EntityVars` | `12` |  |
| <span id="entityrpc"></span>`EntityRpc` | `13` |  |
| <span id="worldstate"></span>`WorldState` | `14` |  |
| <span id="ownerstate"></span>`OwnerState` | `15` |  |
| <span id="containerownership"></span>`ContainerOwnership` | `16` |  |
| <span id="entitystate"></span>`EntityState` | `17` | Per-behavior sync chunks (NetworkTransform/NetworkAnimator) for one entity and tick. |
| <span id="batch"></span>`Batch` | `18` | Gateway -\> client: several reliable messages in one packet: `[count:ushort]{[len:ushort][message]}`. A big mesh produces hundreds of small reliable messages a second (an RPC per shot, a vars update per hit); one packet each was most of a client's packet rate. |
| <span id="instanceprepare"></span>`InstancePrepare` | `19` |  |
| <span id="clientinput"></span>`ClientInput` | `20` |  |
| <span id="serverrpc"></span>`ServerRpc` | `21` |  |
| <span id="instanceready"></span>`InstanceReady` | `22` |  |
| <span id="clientfocushint"></span>`ClientFocusHint` | `23` | Client -\> gateway: where the player is looking ([`ClientFocusHintMsg`](/docs/reference/protocol/client-focus-hint-msg)). A hint, never authority: the gateway drops a non-finite one, rate-limits it and clamps it to the pawn before a policy ever sees it ([`FocusHintFilter`](/docs/reference/interest/focus-hint-filter)). |
| <span id="spawnplayer"></span>`SpawnPlayer` | `30` |  |
| <span id="despawnplayer"></span>`DespawnPlayer` | `31` |  |
| <span id="endsession"></span>`EndSession` | `32` | The session moved to another gateway, so this one must let its client go ([`EndSessionMsg`](/docs/reference/protocol/end-session-msg)). |
| <span id="interestsubscribe"></span>`InterestSubscribe` | `33` | Gateway -\> worker: the set of regions this gateway wants entities from ([`InterestSubscribeMsg`](/docs/reference/protocol/interest-subscribe-msg)). |
| <span id="interestresync"></span>`InterestResync` | `34` | Worker -\> gateway: the last subscription could not be applied; send a Full snapshot ([`InterestResyncMsg`](/docs/reference/protocol/interest-resync-msg)). |
| <span id="entityredirect"></span>`EntityRedirect` | `35` | Worker -\> gateway: an explicitly subscribed entity now lives on another worker ([`EntityRedirectMsg`](/docs/reference/protocol/entity-redirect-msg)). |
| <span id="entityforget"></span>`EntityForget` | `36` | Worker -\> gateway: an entity left every region this gateway subscribes here ([`EntityForgetMsg`](/docs/reference/protocol/entity-forget-msg)). |
| <span id="ghostspawn"></span>`GhostSpawn` | `40` |  |
| <span id="ghoststate"></span>`GhostState` | `41` |  |
| <span id="ghostvars"></span>`GhostVars` | `42` |  |
| <span id="ghostdespawn"></span>`GhostDespawn` | `43` |  |
| <span id="authoritytransfer"></span>`AuthorityTransfer` | `44` |  |
| <span id="forwardinput"></span>`ForwardInput` | `45` |  |
| <span id="authorityrpc"></span>`AuthorityRpc` | `46` |  |
| <span id="ghostsyncstate"></span>`GhostSyncState` | `47` |  |
| <span id="workermessage"></span>`WorkerMessage` | `48` | Game-defined message between two workers: `[kind:ushort][payload]`. Registered per kind with [`NebulaWorker.RegisterMessageHandler`](/docs/reference/worker/nebula-worker#registermessagehandler); sent with [`NebulaWorker.SendToWorker`](/docs/reference/worker/nebula-worker#sendtoworker). |
