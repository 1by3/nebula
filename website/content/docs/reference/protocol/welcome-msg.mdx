---
title: "WelcomeMsg"
description: "Carries the welcome protocol message."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct WelcomeMsg
```

Carries the welcome protocol message.

## Fields

### ClientId [#clientid]

```csharp
public uint ClientId;
```

### TickRate [#tickrate]

```csharp
public byte TickRate;
```

### ServerTick [#servertick]

```csharp
public uint ServerTick;
```

### Identity [#identity]

```csharp
public string Identity;
```

The player's identity for this and every later session ([`PlayerIdentity`](/docs/reference/gateway/player-identity)).

### Token [#token]

```csharp
public string Token;
```

Set when the client presented no token: the anonymous token the gateway issued for [`Identity`](#identity). The client keeps it and presents it in its next [`HelloMsg`](/docs/reference/protocol/hello-msg) to be the same player again. Empty when the client's own token was accepted.

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter w);
```

### Read [#read]

```csharp
public static WelcomeMsg Read(NetworkReader r);
```

