---
title: "JoinRejectReason"
description: "Why a join was refused, in typed form."
---

<div className="text-sm text-fd-muted-foreground">Enum in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public enum JoinRejectReason : byte
```

Why a join was refused, in typed form. A reason string is for the player; this is for the game's client code, which has to tell "your token is bad" (pointless to retry) from "that station is full" (try a queue, another instance, or later). See `docs/capacity-admission.md`.

## Members

| Name | Value | Description |
| --- | --- | --- |
| <span id="none"></span>`None` | `0` | No typed reason was supplied. Read [`JoinRejectedMsg.Reason`](/docs/reference/protocol/join-rejected-msg#reason) and [`JoinRejectedMsg.Retry`](/docs/reference/protocol/join-rejected-msg#retry). |
| <span id="atcapacity"></span>`AtCapacity` | `1` | The target the client asked for is at capacity and the admission hook refused it ([`JoinRejectedMsg.Saturation`](/docs/reference/protocol/join-rejected-msg#saturation) says how full). |
| <span id="denied"></span>`Denied` | `2` | The admission hook refused this particular arrival for its own reasons, with the target below capacity ([`NebulaAdmission.AlwaysConsult`](/docs/reference/gateway/nebula-admission#alwaysconsult)). |
| <span id="protocolunsupported"></span>`ProtocolUnsupported` | `3` | The client's protocol is outside the gateway's supported range ([`JoinRejectedMsg.SupportedMinVersion`](/docs/reference/protocol/join-rejected-msg#supportedminversion)..[`JoinRejectedMsg.SupportedMaxVersion`](/docs/reference/protocol/join-rejected-msg#supportedmaxversion)). The client stops automatic retries; use a client and gateway with matching protocol versions. |
| <span id="contentversionmismatch"></span>`ContentVersionMismatch` | `4` | The client's [`HelloMsg.GameContentVersion`](/docs/reference/protocol/hello-msg#gamecontentversion) is outside the gateway's configured range. The client stops automatic retries. The game defines the content version numbers. |
| <span id="encryptionrequired"></span>`EncryptionRequired` | `5` | The mesh accepts encrypted client links only ([`NebulaConfig.RequireEncryption`](/docs/reference/core/nebula-config#requireencryption)) and this one is in the clear. Retrying without turning encryption on will be refused again. |
