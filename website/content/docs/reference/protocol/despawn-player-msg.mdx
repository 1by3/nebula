---
title: "DespawnPlayerMsg"
description: "Gateway -> worker: the session is over (or the gateway gave up waiting for it to reconnect)."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct DespawnPlayerMsg
```

Gateway -\> worker: the session is over (or the gateway gave up waiting for it to reconnect). Fenced by [`Generation`](#generation) like [`SpawnPlayerMsg`](/docs/reference/protocol/spawn-player-msg).

## Fields

### ClientId [#clientid]

```csharp
public ulong ClientId;
```

### Generation [#generation]

```csharp
public ulong Generation;
```

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter w);
```

### Read [#read]

```csharp
public static DespawnPlayerMsg Read(NetworkReader r);
```

