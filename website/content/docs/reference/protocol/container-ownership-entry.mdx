---
title: "ContainerOwnershipEntry"
description: "Stores the values used by container ownership entry."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/Messages.cs)</div>

```csharp
public struct ContainerOwnershipEntry
```

Stores the values used by container ownership entry.

## Fields

### Instance [#instance]

```csharp
public InstanceContainerInfo Instance;
```

### ContainerIndex [#containerindex]

```csharp
public ushort ContainerIndex;
```

### ContainerId [#containerid]

```csharp
public string ContainerId;
```

### WorkerIndex [#workerindex]

```csharp
public ushort WorkerIndex;
```

### WorkerId [#workerid]

```csharp
public string WorkerId;
```

### Epoch [#epoch]

```csharp
public ulong Epoch;
```

### State [#state]

```csharp
public string State;
```

### HasBounds [#hasbounds]

```csharp
public bool HasBounds;
```

Runtime containers: the box a client registers the container with. See [`LeaseInfo.HasBounds`](/docs/reference/control-plane/lease-info#hasbounds).

### BoundsCenter [#boundscenter]

```csharp
public Vector3 BoundsCenter;
```

### BoundsSize [#boundssize]

```csharp
public Vector3 BoundsSize;
```

### HasPlacement [#hasplacement]

```csharp
public bool HasPlacement;
```

Runtime containers: where the box goes (parent, centre in double, authority, physics frame), carried in the message's trailing placement section so a protocol-18 reader that predates it still reads the float box. [`HasPlacement`](#hasplacement) is false when the sender wrote none (a root with no frame, whose float box is all there is).

### Placement [#placement]

```csharp
public ContainerPlacement Placement;
```

## Properties

### PlacementOrRoot [#placementorroot]

```csharp
public ContainerPlacement PlacementOrRoot { get; }
```

The placement to register with: the trailing one when present, otherwise a root at the float box.

## Methods

### Of [#of]

```csharp
public static ContainerOwnershipEntry Of(LeaseInfo lease, ushort containerIndex, ushort workerIndex);
```

An entry for a lease row, box and placement included.
