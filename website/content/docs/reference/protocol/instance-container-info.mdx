---
title: "InstanceContainerInfo"
description: "Identifies a private container's simulation scope, content, and optional outward view."
---

<div className="text-sm text-fd-muted-foreground">Class in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Protocol/InstanceContainerInfo.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Protocol/InstanceContainerInfo.cs)</div>

```csharp
[Serializable]
public sealed class InstanceContainerInfo
```

Identifies a private container's simulation scope, content, and optional outward view.

## Fields

### InstanceId [#instanceid]

```csharp
public ulong InstanceId;
```

The 64-bit simulation scope ([`NebulaWorker.InstanceKey`](/docs/reference/worker/nebula-worker#instancekey) of [`ScopeKey`](#scopekey)). Zero is the public world.

### ContentResource [#contentresource]

```csharp
public string ContentResource = "";
```

### ObservePublic [#observepublic]

```csharp
public bool ObservePublic;
```

### ObservationCenter [#observationcenter]

```csharp
public Vector3 ObservationCenter;
```

### ObservationSize [#observationsize]

```csharp
public Vector3 ObservationSize;
```

### ScopeKey [#scopekey]

```csharp
public string ScopeKey = "";
```

The opaque scope key the game chose for this instance (`TemplateId/key`, as passed to [`NebulaWorker.PrepareInstance`](/docs/reference/worker/nebula-worker#prepareinstance)), carried so every process can report it in [`EntityLocation.ScopeKey`](/docs/reference/containers/entity-location#scopekey) without inverting the hash. Nebula never parses it. Empty on a lease row written before the key was recorded.

## Methods

### Write [#write]

```csharp
public void Write(NetworkWriter writer);
```

### Read [#read]

```csharp
public static InstanceContainerInfo Read(NetworkReader reader);
```
