---
title: "LeaseState"
description: ""
---

<div className="text-sm text-fd-muted-foreground">Static class in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/ControlPlane/IControlPlane.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/ControlPlane/IControlPlane.cs)</div>

```csharp
public static class LeaseState
```

## Fields

### Assigning [#assigning]

```csharp
public const string Assigning = "assigning";
```

### Active [#active]

```csharp
public const string Active = "active";
```

### Draining [#draining]

```csharp
public const string Draining = "draining";
```

### Orphaned [#orphaned]

```csharp
public const string Orphaned = "orphaned";
```

### Pinned [#pinned]

```csharp
public const string Pinned = "pinned";
```

Dynamic containers only: the orchestrator (or the dashboard) assigned this carried container to a worker of its own, so it no longer follows its carrier. Anything else means "follows the carrier" for such a lease.

## Methods

### IsOwning [#isowning]

```csharp
public static bool IsOwning(string state);
```

The lease names a worker that currently simulates the container.

