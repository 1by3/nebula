---
title: "NebulaMesh"
description: "Developer loop: launch a local mesh from the last build (the standalone orchestrator, which hosts the control plane and starts the gateway and N workers), then press Play in the Editor as a client."
---

<div className="text-sm text-fd-muted-foreground">Static class in namespace `Nebula.Editor` · [Packages/com.1by3.nebula/Editor/NebulaMesh.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Editor/NebulaMesh.cs)</div>

```csharp
public static class NebulaMesh
```

Developer loop: launch a local mesh from the last build (the standalone orchestrator, which hosts the control plane and starts the gateway and N workers), then press Play in the Editor as a client. The CLI (`nebula start`) does the same with more checks; these menu items are the in-Editor shortcut.

## Methods

### StartLocalMesh [#startlocalmesh]

```csharp
[MenuItem("Nebula/Mesh/Start Local Mesh (orchestrator + gateway + workers)", priority = 40)]
public static void StartLocalMesh();
```

### StopLocalMesh [#stoplocalmesh]

```csharp
[MenuItem("Nebula/Mesh/Stop Local Mesh", priority = 41)]
public static void StopLocalMesh();
```

### ResetDevSaves [#resetdevsaves]

```csharp
[MenuItem("Nebula/Dev Loop/Reset Dev Saves", priority = 45)]
public static void ResetDevSaves();
```

Delete the save file the Multiplayer Play Mode dev loop keeps ([`NebulaEditorPersistence.DevSaveFile`](/docs/reference/bootstrap/nebula-editor-persistence#devsavefile)), so the next Play starts from a fresh world. A mesh started with `nebula start` keeps its own data and is not touched.

### OpenDashboard [#opendashboard]

```csharp
[MenuItem("Nebula/Mesh/Open Nebula Dashboard (workers, containers, players)", priority = 43)]
public static void OpenDashboard();
```

### LaunchClient [#launchclient]

```csharp
[MenuItem("Nebula/Mesh/Launch Extra Client Window", priority = 42)]
public static void LaunchClient();
```
