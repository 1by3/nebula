---
title: "ContainerLoad"
description: "What one worker last reported about one container: who is inside it (see WorkerTelemetry)."
---

<div className="text-sm text-fd-muted-foreground">Struct in namespace `Nebula` · [Packages/com.1by3.nebula/Runtime/Orchestrator/AssignmentPolicy.cs](https://github.com/1by3/nebula/blob/main/Packages/com.1by3.nebula/Runtime/Orchestrator/AssignmentPolicy.cs)</div>

```csharp
public struct ContainerLoad
```

What one worker last reported about one container: who is inside it (see [`WorkerTelemetry`](/docs/reference/worker/worker-telemetry)).

## Fields

### Players [#players]

```csharp
public int Players;
```

### Bots [#bots]

```csharp
public int Bots;
```

### ServerDriven [#serverdriven]

```csharp
public int ServerDriven;
```

### Other [#other]

```csharp
public int Other;
```

### Ghosts [#ghosts]

```csharp
public int Ghosts;
```

### Enclosing [#enclosing]

```csharp
public string Enclosing;
```

For a carried (dynamic) container: the container its carrier is in, which may itself be carried. Empty for every other container. Lets a reader fold a vehicle's riders into the box the vehicle is in ([`ScopeLifecycle.Occupancy`](/docs/reference/control-plane/scope-lifecycle#occupancy)) without counting them twice for cost: the counts above stay where the worker simulates the riders.

### WorkerId [#workerid]

```csharp
public string WorkerId;
```

The worker that reported it.

### ReceivedAt [#receivedat]

```csharp
public double ReceivedAt;
```

When the report arrived, on the telemetry clock.

### EntityCostSum [#entitycostsum]

```csharp
public float EntityCostSum;
```

What the worker's own tally of the entities inside came to, each one's category weight times its [`NebulaCost`](/docs/reference/core/nebula-cost) multiplier (docs/cost-telemetry.md). Valid only when [`HasEntityCost`](#hasentitycost); [`CostWeights.Base`](/docs/reference/orchestrator/cost-weights#base) is not in it.

### HasEntityCost [#hasentitycost]

```csharp
public bool HasEntityCost;
```

The report carried an entity cost sum, so [`CostWeights.Of`](/docs/reference/orchestrator/cost-weights#of) uses it instead of counting heads.

## Properties

### Authoritative [#authoritative]

```csharp
public int Authoritative { get; }
```
