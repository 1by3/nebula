---
title: Control plane (SpacetimeDB)
description: Use the SpacetimeDB control plane to register processes, assign containers, and share low-frequency settings.
---

Use the control plane to share process registrations, container assignments, and low-frequency settings across the mesh. The control plane is a SpacetimeDB module in `Packages/com.1by3.nebula/SpacetimeDB/Module~/Lib.cs`. Nebula processes access it through [`IControlPlane`](/docs/reference/control-plane/i-control-plane).

## What it is for, and what it is not

Use it for process registration, heartbeats, container assignments, authority epochs, and shared settings. The orchestrator writes assignments. Workers and gateways register themselves and update their heartbeats. Each process subscribes to the records it needs.

Do not use it for saved entities either. Entities that opt into [persistence](/docs/guides/persistence) are written to a separate SpacetimeDB database, `nebula-persist`, by the module in `Packages/com.1by3.nebula/SpacetimeDB/PersistenceModule~`. The two databases are kept apart so that wiping the control plane's registry never touches a saved world.

Do not use the control plane for per-tick state. Workers exchange transforms, input, RPCs, and ghost state over their direct links and through the gateway. Simulation ticks do not wait for database operations.

If the control-plane connection fails, workers continue with the last assignments they received. `SpacetimeControlPlane` attempts to reconnect every two seconds and discards writes while disconnected.

During the outage, the orchestrator does not reassign containers, declare workers dead, or start replacements. Handovers can continue between connected workers because they use the last known assignments.

## The module

The module exposes five public tables. Reducers use `ctx.Timestamp` for each SpacetimeDB `Timestamp`, so the records use one clock.

| Table | Primary key | Columns |
| --- | --- | --- |
| `worker` | `WorkerId` | `WorkerIndex`, `Address`, `Port`, `Status`, `LastHeartbeat`, `TickCount`, `TickMs`, `EntityCount`, `AuthoritativeCount`, `GhostCount`, `PlayerCount`, `BotCount`, `ServerDrivenCount` |
| `container_lease` | `ContainerId` | `WorkerId` (`""` = unassigned), `Epoch`, `State`, `UpdatedAt` |
| `gateway` | `GatewayId` | `Address`, `Port`, `LastHeartbeat` |
| `orchestrator` | `OrchestratorId` | `LastHeartbeat`, `DesiredWorkers` |
| `game_setting` | `Key` | `Value`, `Version` (bumped on every change), `UpdatedAt` |

The orchestrator assigns each worker a `WorkerIndex`. This value becomes the high 16 bits of network IDs created by that worker and selects its diagnostic color.

Each container has an `Epoch` that increases when its assigned worker changes. Workers and the gateway use it to reject state from a previous assignment.

### Reducers

| Reducer | Arguments | Effect |
| --- | --- | --- |
| `RegisterWorker` | `workerId, workerIndex, address, port` | Insert or update the row with status `starting` and a fresh heartbeat; counters reset to 0 on insert. |
| `HeartbeatWorker` | `workerId, status, tickCount, tickMs, entityCount, authoritativeCount, ghostCount, playerCount, botCount, serverDrivenCount` | Update status, heartbeat and every counter. No-op for an unknown id. |
| `UnregisterWorker` | `workerId` | Delete the row and set every lease it held to `WorkerId = ""`, state `orphaned`. |
| `RegisterGateway` | `gatewayId, address, port` | Insert or update. |
| `HeartbeatGateway` | `gatewayId` | Refresh the heartbeat. |
| `UnregisterGateway` | `gatewayId` | Delete the row. |
| `HeartbeatOrchestrator` | `orchestratorId, desiredWorkers` | Insert or update; lets workers and gateways tell whether somebody is driving the mesh. |
| `EnsureContainer` | `containerId` | Insert an `orphaned` lease at epoch 0 if none exists. |
| `AssignContainer` | `containerId, workerId` | Set the owner, bump the epoch, state `active`. A no-op when the same worker already holds it active, so no epoch is burned. Inserts at epoch 1 if the row is missing. |
| `SetLeaseState` | `containerId, state` | Overwrite the state string. |
| `ReleaseContainer` | `containerId` | `WorkerId = ""`, state `orphaned`. |
| `RemoveContainer` | `containerId` | Delete the lease row. Workers call it when the carrier of a [dynamic container](/docs/guides/dynamic-containers) despawns. |
| `SetGameSetting` | `key, value` | Insert at version 1, or update and bump the version. No-op when the value is unchanged. |
| `ResetControlPlane` | none | Delete every row of every table. The orchestrator calls it at startup (`-nebula-reset`, default on). |

### Lease states and worker statuses

`LeaseState` and `WorkerStatus` in `IControlPlane.cs` are the string constants the module uses:

| Lease state | Meaning |
| --- | --- |
| `assigning` | Reserved for a staged transfer. Not produced by the current orchestrator; `AssignContainer` goes straight to `active`. |
| `active` | The worker in `WorkerId` owns the container at `Epoch`. |
| `draining` | Reserved. Not produced; retirement reuses the per-entity handover path with the lease staying `active` until it moves. |
| `orphaned` | No owner. Set by `EnsureContainer`, `ReleaseContainer` and `UnregisterWorker`; the next assignment pass deals it out. |
| `pinned` | Dynamic containers only: the orchestrator gave this carried container a worker of its own, so it no longer follows its carrier. Set through the dashboard or `POST /api/containers/pin`. |

| Worker status | Meaning |
| --- | --- |
| `starting` | Registered, not yet ticking. Set by `RegisterWorker`. |
| `ready` | Simulating. The status the worker's heartbeat reports. |
| `draining` | Reserved for a worker that is handing everything over. Not reported; the orchestrator tracks retirement on its own side. |
| `dead` | Reserved. `IsWorkerAlive` returns false for it regardless of heartbeat age, but nothing writes it: the orchestrator unregisters a dead worker instead. |

Liveness is `now - LastHeartbeat <= WorkerTimeoutSeconds` (default 5 s), evaluated by the orchestrator against `IControlPlane.Now`, which `SpacetimeControlPlane` estimates from the newest timestamp it has seen plus local elapsed time.

## IControlPlane

Every sim process holds one `IControlPlane`. It exposes mirrored lists (`Workers`, `Leases`, `Gateways`, `Settings`), a `Changed` event, and one method per reducer. All callbacks fire on the main thread from `Tick()`.

```csharp
public interface IControlPlane : IDisposable
{
    bool IsConnected { get; }
    DateTime Now { get; }
    event Action Changed;

    IReadOnlyList<WorkerInfo> Workers { get; }
    IReadOnlyList<LeaseInfo> Leases { get; }
    IReadOnlyList<GatewayInfo> Gateways { get; }
    IReadOnlyDictionary<string, string> Settings { get; }

    void Connect();
    void Tick();

    void RegisterWorker(string workerId, uint workerIndex, string address, ushort port);
    void HeartbeatWorker(string workerId, string status, in WorkerStats stats);
    void UnregisterWorker(string workerId);
    void RegisterGateway(string gatewayId, string address, ushort port);
    void HeartbeatGateway(string gatewayId);
    void UnregisterGateway(string gatewayId);
    void HeartbeatOrchestrator(string orchestratorId, uint desiredWorkers);
    void SetSetting(string key, string value);
    void EnsureContainer(string containerId);
    void AssignContainer(string containerId, string workerId);
    void SetLeaseState(string containerId, string state);
    void ReleaseContainer(string containerId);
    void ResetControlPlane();
}
```

`ControlPlaneExtensions` adds `FindWorker`, `FindLease`, `GetSetting`, `GetSettingInt` and `IsWorkerAlive`.

### SpacetimeControlPlane

[`SpacetimeControlPlane`](/docs/reference/control-plane/spacetime-control-plane) connects Nebula to SpacetimeDB. It subscribes to the control-plane tables and refreshes its local lists after a row changes. It raises `Changed` once on the next `Tick()`.

Write operations call the generated reducer methods in `Nebula.Spacetime`. Nebula logs a reducer failure as `control plane reducer failed: ...`.

The URI and database come from `NebulaConfig.SpacetimeUri` / `SpacetimeDatabase` (defaults `http://127.0.0.1:3000` and `nebula`), overridden by `-nebula-spacetime <uri>` and `-nebula-database <name>`; the orchestrator passes both to every role it launches.

### LocalControlPlane

[`LocalControlPlane`](/docs/reference/control-plane/local-control-plane) keeps control-plane state inside one process. It follows the same lease and epoch rules as the SpacetimeDB implementation. Use it for automated tests or other single-process runs.

Select it with `NebulaConfig.UseLocalControlPlane` or `-nebula-local-control-plane`. Because it is in-process, nothing outside the process can see it: the orchestrator skips launching the gateway, reconciling the worker count and relaunching workers when it is on. It is a test harness, not a way to run a mesh without SpacetimeDB.

## Mesh settings

Use `game_setting` rows for values that every worker needs but that do not change every tick. Nebula stores each setting as a string key and value. Your game defines what it means.

- Seed settings when the orchestrator starts with `-nebula-settings key=value,key=value`.
- Edit settings from the dashboard or with `POST /api/settings`.
- Call `ControlPlane.SetSetting` from game code on a worker.
- Read settings from `IControlPlane.Settings` after the `Changed` event. Use `GetSetting` or `GetSettingInt` to supply a fallback value.

Nebula does not provide a singleton gameplay worker. If each worker must respond to a shared value, make the operation idempotent and let each worker reconcile its authoritative entities. See [Spawning and mesh settings](/docs/guides/spawning#coordinate-work-from-a-setting).

## Building, publishing and regenerating bindings

`Module~/` is the module source (C#, .NET 10, compiled to WASM). The trailing `~` hides it from Unity's asset pipeline so its sources are not compiled into the game. `Generated/` holds the client bindings in the `Nebula.Spacetime` namespace, produced by `spacetime generate`. Regenerate them whenever `Lib.cs` changes; the runtime will not compile against stale bindings.

```bash
cd Packages/com.1by3.nebula/SpacetimeDB/Module~
spacetime build
spacetime generate --lang csharp --module-path . --out-dir ../Generated --namespace Nebula.Spacetime -y
spacetime publish -s local nebula
```

The Editor menu runs the same commands: **Nebula > Control Plane > Start SpacetimeDB (local)** (`spacetime start --data-dir Temp/spacetimedb --listen-addr 127.0.0.1:3000`), **Publish Module** (`spacetime publish -s local <database> --delete-data -y`), **Regenerate C# Bindings** and **Stop SpacetimeDB**.

### What `nebula start` does with it

`nebula start` pings `mesh.spacetimeUri` from `nebula.json`; if nothing answers it starts a local SpacetimeDB (data in `Temp/spacetimedb`, log in `Builds/<platform>/Logs/spacetimedb.log`) and remembers that it did so `nebula stop` can stop it again. It then publishes the module as `mesh.database` with `--delete-data`, so every run starts from empty tables. `--skip-publish` keeps the published module and data.

The control plane holds only ephemeral registry state, so wiping it is always safe while the mesh is down. The orchestrator's `ResetControlPlane` at startup covers the case where the module was not republished.

`nebula start` then publishes the persistence module as `mesh.persistenceDatabase` *without* `--delete-data`, so saved entities survive the restart. Use `nebula start --reset-persistence` when you want an empty world as well. See [Persistence](/docs/guides/persistence).

### Maincloud for deployment

For deployment, configure a SpacetimeDB server that every virtual machine can reach. Run `nebula config spacetime` to sign in to Maincloud at `https://maincloud.spacetimedb.com` or record another server URL. Set a unique database name in `deploy.database` in `nebula.json`.

When you run `nebula deploy`, the CLI publishes the module before it creates virtual machines. It passes the server and database to the orchestrator, which passes them to the workers and gateway. Add `--reset-control-plane` to publish with `--delete-data`.

The CLI publishes the persistence module to the same server as `deploy.persistenceDatabase` in `nebula.json`, which defaults to the control-plane database name with a `-persist` suffix. It keeps its rows unless you add `--reset-persistence`.

<Callout type="warn" title="Reducers accept any caller">
The module does not authenticate reducer callers. Anyone who can connect to the database can register a worker, change a container assignment, or rewrite a setting. Do not treat the database name as a security boundary. Restrict access with deployment infrastructure until you add authentication that meets your game's requirements.
</Callout>

<Cards>
  <Card title="Orchestrator and Nebula Dashboard" href="/docs/guides/orchestrator-and-dashboard" description="Who writes the leases and when." />
  <Card title="Deploying to Hetzner" href="/docs/deploy/hetzner" description="Maincloud plus one VM per worker." />
  <Card title="Persistence" href="/docs/guides/persistence" description="The second database, and the entities that outlive a worker." />
  <Card title="Architecture" href="/docs/concepts/architecture" description="Why the control plane must never be a hot-path dependency." />
</Cards>
